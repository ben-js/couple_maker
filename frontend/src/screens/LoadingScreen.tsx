import React from 'react';
import { View, Text } from 'react-native';

const LoadingScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>LoadingScreen (로딩 화면)</Text>
  </View>
);

export default LoadingScreen; 